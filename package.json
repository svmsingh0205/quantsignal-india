{
  "name": "quantsignal-tests",
  "version": "1.0.0",
  "description": "Playwright UI tests for QuantSignal India",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
